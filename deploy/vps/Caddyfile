# Caddyfile template for deploy/vps
# Replace DOMAIN with your real domain (script will do this for you)

DOMAIN {
  encode gzip zstd
  reverse_proxy app:3000
  log {
    output file /data/access.log
  }
}
