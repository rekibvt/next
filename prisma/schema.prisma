datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

// Garde tes modèles (User, etc.) en dessous
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?Ò
}